# ─── Backend Environment Variables ────────────────────────────────────────────
# Copy this file to .env and fill in real values. Never commit .env to git.

# Server
PORT=5000
NODE_ENV=development
FRONTEND_URL=http://localhost:5173

# MongoDB
MONGO_URI=mongodb+srv://<username>:<password>@cluster.mongodb.net/slienx?retryWrites=true&w=majority

# ─── Firebase Admin SDK credentials ───────────────────────────────────────────
# Option A (recommended for Render / Railway / Docker):
#   Paste the entire contents of your service-account JSON file, base-64 encoded:
#   $ base64 -i serviceAccountKey.json | tr -d '\n'
#   Then set:
FIREBASE_SERVICE_ACCOUNT_JSON=<base64-encoded-service-account-json>

# Option B (local dev with ADC):
#   Point to your service-account JSON file on disk:
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/serviceAccountKey.json

# Firebase project (required for Option B / ADC without project hint)
FIREBASE_PROJECT_ID=your-firebase-project-id
